proc add()
    mult()
    while true
        if token = '+'
            advance()
            mult()
        else if token = '-'
            advance()
            mult()
        else break
    end
end